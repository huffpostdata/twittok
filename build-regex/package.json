{
  "name": "build-regex",
  "version": "1.0.0",
  "description": "To build regular expressions:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "regexpu": "^3.3.0",
    "sync-request": "^3.0.1"
  }
}
